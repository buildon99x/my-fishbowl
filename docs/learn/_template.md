# <YYYY-MM-DD> <Topic>

## Symptom

- 어떤 화면/조작에서 어떤 잘못된 결과가 나타났는지 작성한다.
- 가능하면 콘솔 오류, 시각적 차이, 사용자 보고 문구를 함께 적는다.

## Root Cause

- 잘못된 동작을 일으킨 직접 원인을 작성한다.
- "이전 구현의 부수효과에 의존했다", "두 모듈에 같은 이름의 상태가 있었다" 처럼 구조적 원인을 함께 적는다.

## Fix

- 어떤 파일/함수를 어떻게 바꿔서 해결했는지 작성한다.
- 부분 수정이라면 남아 있는 후속 작업을 함께 적는다.

## Prevention Rule

- 다음에 같은 실수를 막기 위한 체크리스트 항목 또는 작업 절차를 한 줄로 작성한다.
- 가능하면 `docs/spec-command-patterns.md`의 어떤 체크리스트로 옮길지 명시한다.

## Related Files

- 원인/수정에 관련된 파일과 함수 경로를 나열한다. (`src/main.js`의 `renderFishes` 등)
